# This will enable WSL and install Ubuntu 20.4 by default
wsl --install

# A reboot is now required, upon reboot a Windows Console will prompt the user to create a user account; however,
# tt would be ideal  to create the WSL  Ubuntu user without requiring any additional input from the user
  